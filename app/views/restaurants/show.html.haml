
.container
  .restaurant-cover
    .restaurant-label=@restaurant.name
    =image_tag "food-mobile.jpg"

  %ul
    -@restaurant.items.each do |item|
      %li="#{item.name}: #{@ratings_hash[item.id]}"
